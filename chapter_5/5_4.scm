;; > rather painful without that kind of *generic* support
;; IMHO at least up to now, all things can be done in SICP without generic but just using cond to dispatch.
;; So I don't know what the author wants for this exercise.

;; https://simonmar.github.io/bib/papers/eval-apply.pdf
;; unrelated where Push/Enter is just how C works (stack model).

;; Anyway no close relations with  programming strategy (see SICP_SDF/SDF_exercises/README.md), so fine to skip temporarily.
